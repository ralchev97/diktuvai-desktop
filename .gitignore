node_modules/
dist/
release/
*.log
.DS_Store
.env
*.db

# Apple Developer certs (NEVER commit)
build/certs/
*.p8
*.cer
*.key
*.certSigningRequest
*.p12
.env.signing

# Native addon build artifacts
native/fn-key/build/
SIGNING_NOTES.md

# Vite timestamp cache files (regenerated on every dev start)
electron.vite.config.*.mjs

# Accidentally-created literal "~/" directory (should be $HOME)
/~/
